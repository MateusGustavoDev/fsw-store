export type Product = {
    id: string
    name: string
    slug: string
    description: string
    imageUrls: string[]
    basePrice: number
    categoryId: number
    discountPercentage: number
  }
  